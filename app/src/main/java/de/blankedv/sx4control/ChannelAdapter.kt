package de.blankedv.sx4control

import android.view.View
import android.view.ViewGroup
import android.widget.BaseAdapter
import android.widget.TextView

import org.jetbrains.anko.layoutInflater

/*
class ChannelAdapter : BaseAdapter() {


    // override other abstract methods here


    override fun getView(position: Int, convertView: View?, container: ViewGroup): View {
        var convertView = convertView
        if (convertView == null) {
            convertView = getLayoutInflater().inflate(R.layout.channel_item, container, false)
        }

        (convertView.findViewById<View>(android.R.id.text1) as TextView).text = getItem(position)
        return convertView
    }

    override fun getCount(): Int {
        return 0
    }

    override fun getItem(item: Int): String {

        return "no yet defined"
    }

    override fun getItemId(position: Int): Long {
        return 0
    }

}
*/